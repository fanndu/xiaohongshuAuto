import { defineConfig } from 'wxt';

export default defineConfig({
  manifestVersion: 3,
  manifest: {
    name: '小红书个人资料采集器',
    description: '采集小红书用户个人资料并导出为 Excel 文件。',
    version: '0.1.0',
  },
});
